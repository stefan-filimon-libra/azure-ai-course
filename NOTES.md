1. Embeddings dimensions: 1536
2. The score is 0.1055. This tells us that the vector database always returns a result, but the very low score shows that the text is completely irrelevant to our question.
3. When use_rag is true, a CONTEXT section is added directly into the prompt_sent. This section contains the text passages retrieved from the database, along with their reference numbers (e.g., [1]) and match scores. Additionally, the system_prompt receives extra instructions telling the AI to base its answer strictly on those passages and to cite them.
4. The field says "runs_on": "unknown". It is supposed to run on both, but because I ran the app in Docker, it uses an API key. The Azure Agent Service strictly requires az login (Entra ID) authentication to connect, which Docker doesn't have.